[package]
name = "farms-interface"
version = "1.0.0"
edition = { workspace = true }
license = { workspace = true }
publish = false
description = "Farms contract client interface for cross-contract calls"

[lib]
crate-type = ["lib"]
doctest = true

[dependencies]
soroban-sdk = { workspace = true }
