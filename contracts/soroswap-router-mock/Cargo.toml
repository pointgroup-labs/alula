[package]
name = "soroswap-router-mock"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
soroswap-library = "^2.0.0"

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }